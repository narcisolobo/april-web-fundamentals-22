function nextImg(element) {
  element.src = "./images/pixel-ninjas-2.png";
}

function prevImg(element) {
  element.src = "./images/stonepunk.png";
}

function increase(element) {
  element.innerText++;
}